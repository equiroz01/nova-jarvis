# ══════════════════════════════════════════════════
# N.O.V.A. — MCP Server Configuration
# Add your MCP servers here or configure via the UI
# ══════════════════════════════════════════════════

servers:
  # Example: GitHub MCP
  # - name: "GitHub"
  #   description: "Access GitHub repos, issues, PRs"
  #   type: "stdio"  # stdio or sse
  #   command: "npx"
  #   args: ["-y", "@modelcontextprotocol/server-github"]
  #   env:
  #     GITHUB_TOKEN: "your-token"
  #   enabled: true

  # Example: Filesystem MCP
  # - name: "Filesystem"
  #   description: "Read/write local files"
  #   type: "stdio"
  #   command: "npx"
  #   args: ["-y", "@modelcontextprotocol/server-filesystem", "/Users/hnl-mac15/Documents"]
  #   enabled: true

  # Example: Brave Search MCP
  # - name: "Brave Search"
  #   description: "Web search via Brave"
  #   type: "stdio"
  #   command: "npx"
  #   args: ["-y", "@modelcontextprotocol/server-brave-search"]
  #   env:
  #     BRAVE_API_KEY: "your-key"
  #   enabled: false
